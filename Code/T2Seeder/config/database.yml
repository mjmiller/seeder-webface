  development:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: thesortus_t2prime
    pool: 5
    username: root
    password:
    socket: /tmp/mysql.sock

# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
  test:
    adapter: mysql2
    encoding: utf8
    reconnect: false
    database: thesortus_t2_test
    pool: 5
    username: root
    password:
    socket: /tmp/mysql.sock
